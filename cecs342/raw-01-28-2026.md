i need tod ownlaod haskell from haskell.org
make some simpole script to jsut do the hello wordl equivilent

**Haskell toolchain:**

- ghcup: installer and version manager for Haskell toolchain (GHC, Cabal, Stack, HLS)
- stack: project build tool, manages dependencies using reproducible snapshots, popular for teaching
- hls (Haskell Language Server): IDE backend, provides type info, errors, code navigation via LSP
- cabal: build system and package manager (cabal build compiles, cabal install installs packages)
- ghc (Glasgow Haskell Compiler): the Haskell compiler, turns source into executables/libraries
- ghci: interactive REPL for GHC, lets you load modules, test expressions, inspect types

for ghci

- all commands start with :, :? for help
  :load is important to load a file
  :reload to prolly update changes
  then lteraly type the functiuons to execute liek twice 6

one example of how anyhtin gyou do in c you can do in haskell
in c:
// Conditionals
int signum0(int n) {
    if (n < 0) return -1;
    if (n > 0) return 1;
    return 0;
}

or like this... the ternary si a prue function because everything is computed from input -> one output
(most simialr to haskel, its a pure function so we must be able to do it in haskel)
int signum2(int n) {
    return (n<0) ? -1 : ((n>0) ? 1 : 0);
}


in haskell its pretty nice
-- Conditionals
signum1 n = if      n < 0 then -1
            else if n > 0 then  1
            else                0

-- Piece-wise functions with guards
signum2 n | n < 0     = -1
          | n > 0     =  1
          | otherwise =  0

